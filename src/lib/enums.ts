export enum ActiveButton {
    dashboard = "Dashboard",
    transactions = "Transactions",
    categories = "Categories",
    budget = "Budget"
}

export enum Type {
  expense = "Expense",
  income = "Income"
}

export enum Status {
  done = "Done",
  processing = "processing",
  failed = "failed"
}